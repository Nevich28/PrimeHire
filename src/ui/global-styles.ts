/**
 * Global stylesheet loader.
 *
 * Native has no global stylesheet, so this is deliberately empty; the web
 * implementation next to it imports the CSS. Keeping the split in the filename
 * means neither platform has to check which one it is at runtime.
 */
export {};
